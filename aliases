# This file defines aliases shared amongst all shells
#
# Use the shell script ~/.config/bin/get-aliases to get a
# formatted list of aliases without comments

# cd shortcuts
..    = cd ..
...   = cd ../..

# git shortcuts
gp    = git pull
gpp   = git pullpush
gs    = git st

# etc
rs    = rails start
t     = tmux
v     = vim

# linux
lock    = i3lock -t -i /home/alex/img.png
pbcopy  = xclip -i -selection clipboard
pbpaste = xclip -o -selection clipboard

# macos
airport = /System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport
ql      = quicklook
