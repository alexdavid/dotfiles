# This file defines aliases shared amongst all shells
#
# Use the shell script ~/.config/bin/get-aliases to get a
# formatted list of aliases without comments

# cd shortcuts
..    = cd ..
...   = cd ../..

# git shortcuts
gp    = git pull
gpp   = git pullpush
gs    = git status

# etc
ql    = quicklook
rs    = rails start
t     = tmux new "vim +qall; reattach-to-user-namespace -l $SHELL"
ts    = switch-theme
