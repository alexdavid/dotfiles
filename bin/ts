#!/bin/bash

if grep -q "dark" ~/.config/colors; then echo "light" > ~/.config/colors; else echo "dark" > ~/.config/colors; fi

if [ "$UNAME" = Darwin ]; then
  osascript ~/.config/osx/set_solarized.applescript
fi
vim +qall
