#!/usr/bin/env sh

DESKTOP_FILES=(~/.local/share/applications/*.desktop /usr/share/applications/*.desktop)
RESULT=$(
  printf "%s\n" "${DESKTOP_FILES[@]}" \
  | xargs -I{} basename {} \
  | bemenu --prompt "Launch"
)
[ -n "$RESULT" ] && gtk-launch "$RESULT"
