# Keys
bind-key G end
bind-key g home
bind-key h quit
bind-key j next
bind-key k prev
bind-key l open
bind-key q hard-quit

include ~/.local/share/newsboat/credentials

# Misc
browser ~/.config/bin/browser
feed-sort-order title
notify-program "notify-send"
refresh-on-startup yes
