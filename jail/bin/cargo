#!/usr/bin/env sh
source ~/.config/jail/init

mkdir -p ~/.local/share/rust/{rustup,cargo}

bind ~/.local/share/rust/cargo ~/.cargo
bind ~/.local/share/rust/rustup ~/.rustup
bind ~/.local/share/rust/cache ~/.cache
gpu
gui
mount_cwd
network

run_jail
